---
import * as cheerio from "cheerio";

type Props = {
  src: string; // INFO: SVG string
  class?: string;
};

const { src } = Astro.props;

const $ = cheerio.load(src ?? "");
$("svg").addClass(Astro.props.class);
---

<Fragment set:html={$.html("svg")} />
