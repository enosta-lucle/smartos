---
type Props = {
  value: number;
  label: string;
};

const { value, label } = Astro.props;
---

<div data-aos="count" class="flex">
  <span>{label}</span>
</div>

<style define:vars={{ count: value }}>
  @property --num {
    syntax: "<integer>";
    initial-value: 0;
    inherits: false;
  }

  div {
    transition: --num 50s;
    counter-set: num var(--num);

    &.aos-animate {
      --num: var(--count);
    }

    &::before {
      content: counter(num);
    }
  }
</style>
